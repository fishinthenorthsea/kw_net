Private Sub Workbook_BeforeSave(ByVal SaveAsUI As Boolean, Cancel As Boolean)
    Dim str As String
    Dim bookName As String
    bookName = ActiveWorkbook.Name
    str = "python autoCsv.py -f "
    str = str + bookName
    Call Shell(str)
    Debug.Print str
End Sub


